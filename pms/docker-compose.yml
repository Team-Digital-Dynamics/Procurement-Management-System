services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: pms
      MYSQL_USER: pms
      MYSQL_PASSWORD: pms
      MYSQL_ROOT_PASSWORD: admin
    ports:
      - "3307:3306"
    volumes:
      - pms_mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-upms", "-ppms"]
      interval: 10s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/pms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: pms
      SPRING_DATASOURCE_PASSWORD: pms
<<<<<<< Updated upstream:pms/docker-compose.yml
      PMS_JWT_SECRET: ${PMS_JWT_SECRET:?PMS_JWT_SECRET is required}
      PMS_MAIL_HOST: ${PMS_MAIL_HOST:-mail.dalesmith.co.za}
      PMS_MAIL_PORT: ${PMS_MAIL_PORT:-587}
      PMS_MAIL_PROTOCOL: ${PMS_MAIL_PROTOCOL:-smtp}
      PMS_MAIL_USERNAME: ${PMS_MAIL_USERNAME:-digitaldynamics@dalesmith.co.za}
      PMS_MAIL_PASSWORD: ${PMS_MAIL_PASSWORD:-}
=======
      PMS_JWT_SECRET: ${PMS_JWT_SECRET:?dev-jwt-secret-change-me-32-bytes-min}
      PMS_MAIL_HOST: smtp.gmail.com
      PMS_MAIL_PORT: 587
      PMS_MAIL_PROTOCOL: smtp
      PMS_MAIL_USERNAME: escgroupin4@gmail.com
      PMS_MAIL_PASSWORD: zwfklulnkhjjjcwr

>>>>>>> Stashed changes:docker-compose.yml
    ports:
      - "8080:8080"

volumes:
  pms_mysql:
